<UserControl x:Class="Sentinel.Gui.Views.FilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Sentinel.Gui">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Files"/>
            <TextBlock Text="File-level scanner: hashes, Authenticode, PE features, entropy, MOTW, ADS. Live streaming results."
                       Foreground="{StaticResource DimTextBrush}" Margin="0,0,0,12"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox x:Name="PathBox" Width="420" VerticalContentAlignment="Center"/>
            <Button x:Name="BrowseBtn" Content="Browse…" Click="Browse_Click"/>
            <Button x:Name="ScanBtn" Content="Scan" Click="ScanPath_Click"/>
            <TextBlock x:Name="StatusText" Foreground="{StaticResource DimTextBrush}" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="FilesGrid" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Path" Binding="{Binding Path, Converter={StaticResource TruncateConverter}}" Width="*"/>
                <DataGridTextColumn Header="Size" Binding="{Binding Size, Converter={StaticResource BytesConverter}}" Width="80"/>
                <DataGridTextColumn Header="SHA-256" Binding="{Binding Sha256, Converter={StaticResource TruncateConverter}}" Width="150"/>
                <DataGridTextColumn Header="Signature" Binding="{Binding SignatureStatus}" Width="120"/>
                <DataGridTextColumn Header="Signer" Binding="{Binding SignerName, Converter={StaticResource TruncateConverter}}" Width="150"/>
                <DataGridTextColumn Header="Entropy" Binding="{Binding Entropy, StringFormat={}{0:0.00}}" Width="70"/>
                <DataGridTextColumn Header="MOTW" Binding="{Binding HasMotw}" Width="60"/>
                <DataGridTextColumn Header="ADS" Binding="{Binding AdsCount}" Width="50"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>