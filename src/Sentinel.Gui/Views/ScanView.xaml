<UserControl x:Class="Sentinel.Gui.Views.ScanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Sentinel.Gui">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Scan"/>
            <TextBlock Text="Select a target and run a scan. Results stream in live; findings are correlated and persisted."
                       Foreground="{StaticResource DimTextBrush}" Margin="0,0,0,12"/>
        </StackPanel>

        <!-- Controls -->
        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="16,12">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="QuickBtn" Content="Quick scan" Click="Quick_Click" Margin="0,0,8,0"/>
                    <Button x:Name="FullBtn" Content="Full scan" Click="Full_Click" Margin="0,0,8,0"/>
                    <Button x:Name="CancelBtn" Content="Cancel" Click="Cancel_Click" IsEnabled="False"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <TextBox x:Name="PathBox" Width="420" VerticalContentAlignment="Center"/>
                    <Button x:Name="BrowseBtn" Content="Browse…" Click="Browse_Click"/>
                    <Button x:Name="ScanPathBtn" Content="Scan path" Click="ScanPath_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Progress -->
        <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="16,12" Margin="0,12,0,0">
            <StackPanel>
                <DockPanel>
                    <TextBlock x:Name="ProgressState" Text="Idle" FontWeight="SemiBold" DockPanel.Dock="Left"/>
                    <TextBlock x:Name="ProgressDetail" Text="" Foreground="{StaticResource DimTextBrush}" DockPanel.Dock="Right"/>
                </DockPanel>
                <ProgressBar x:Name="ProgressBar" Height="8" Margin="0,8,0,0" Minimum="0" Maximum="100"
                             Foreground="{StaticResource AccentBrush}" Background="#FF21262D" BorderThickness="0"/>
                <TextBlock x:Name="ProgressSub" Text="" Foreground="{StaticResource DimTextBrush}" FontSize="11" Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <!-- Live results -->
        <Border Grid.Row="3" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="16,12" Margin="0,12,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Live results" Style="{StaticResource SectionTitleStyle}" FontSize="15"/>
                <DataGrid x:Name="ResultsGrid" IsReadOnly="True" Margin="0,8,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Path" Binding="{Binding Path, Converter={StaticResource TruncateConverter}}" Width="*"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding Size, Converter={StaticResource BytesConverter}}" Width="80"/>
                        <DataGridTextColumn Header="SHA-256" Binding="{Binding Sha256, Converter={StaticResource TruncateConverter}}" Width="140"/>
                        <DataGridTextColumn Header="Signature" Binding="{Binding SignatureStatus}" Width="110"/>
                        <DataGridTextColumn Header="Entropy" Binding="{Binding Entropy, StringFormat={}{0:0.00}}" Width="70"/>
                        <DataGridTextColumn Header="MOTW" Binding="{Binding HasMotw}" Width="60"/>
                        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>