<UserControl x:Class="Sentinel.Gui.Views.PersistenceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Sentinel.Gui">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Persistence"/>
            <TextBlock Text="Autoruns-style enumeration: Run keys, startup folders, services, scheduled tasks, WMI subscriptions, browser extensions."
                       Foreground="{StaticResource DimTextBrush}" Margin="0,0,0,12"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="ScanBtn" Content="Rescan persistence" Click="Scan_Click"/>
            <TextBlock x:Name="CountText" Foreground="{StaticResource DimTextBrush}" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="PersGrid" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="110"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Command" Binding="{Binding Command}" Width="*"/>
                <DataGridTextColumn Header="Target" Binding="{Binding TargetPath, Converter={StaticResource TruncateConverter}}" Width="200"/>
                <DataGridTextColumn Header="Exists" Binding="{Binding TargetExists}" Width="60"/>
                <DataGridTextColumn Header="Enabled" Binding="{Binding IsEnabled}" Width="70"/>
                <DataGridTextColumn Header="User" Binding="{Binding User}" Width="90"/>
                <DataGridTextColumn Header="Signer" Binding="{Binding Signer}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>