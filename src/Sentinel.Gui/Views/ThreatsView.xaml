<UserControl x:Class="Sentinel.Gui.Views.ThreatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Sentinel.Gui">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Threats"/>
            <TextBlock Text="Correlated findings with risk scores. Evidence-driven; a finding always has backing evidence."
                       Foreground="{StaticResource DimTextBrush}" Margin="0,0,0,12"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="RefreshBtn" Content="Refresh" Click="Refresh_Click"/>
            <Button x:Name="MarkReviewedBtn" Content="Mark selected reviewed" Click="MarkReviewed_Click"/>
            <Button x:Name="QuarantineBtn" Content="Quarantine selected file" Click="Quarantine_Click"/>
            <TextBlock x:Name="CountText" Foreground="{StaticResource DimTextBrush}" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="FindingsGrid" IsReadOnly="True" SelectionChanged="FindingsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="{Binding Severity, Converter={StaticResource SeverityToBrush}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Score" Binding="{Binding RiskScore, StringFormat={}{0:0.0}}" Width="60"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Entity" Binding="{Binding EntityKey, Converter={StaticResource TruncateConverter}}" Width="220"/>
                <DataGridTextColumn Header="MITRE" Binding="{Binding MitreTacticsJson}" Width="140"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/>
                <DataGridTextColumn Header="First seen" Binding="{Binding FirstSeenUtc, Converter={StaticResource UtcTimeConverter}}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Evidence panel for selected finding -->
        <TextBlock Grid.Row="3" Text="Evidence" Style="{StaticResource SectionTitleStyle}" FontSize="14" Margin="0,16,0,4"/>
        <DataGrid Grid.Row="4" x:Name="EvidenceGrid" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="70"/>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, Converter={StaticResource UtcTimeConverter}}" Width="150"/>
                <DataGridTextColumn Header="Event" Binding="{Binding Event}" Width="160"/>
                <DataGridTextColumn Header="Explanation" Binding="{Binding Explanation}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>